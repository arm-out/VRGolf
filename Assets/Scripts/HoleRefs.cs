using UnityEngine;

public class HoleRefs : MonoBehaviour
{
    public GameObject holeTrigger;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
